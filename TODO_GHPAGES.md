# GitHub Pages Deployment TODO

- [ ] Ensure GitHub Pages source is set to `gh-pages` branch / root (/) in repo Settings.
- [ ] Create and/or verify `homepage` in `package.json` matches GitHub Pages URL.
- [ ] Confirm build succeeds locally: `npm run build`.
- [ ] Push changes to `main` so workflow deploys (or run deploy script locally with `gh-pages`).
- [ ] Verify deployed site loads at `https://greyengravings.github.io/bites-bombay-sandwich/`.

